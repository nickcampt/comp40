#ifndef IO_H
#define IO_H

/*
   these most likely do not need the a and b parameters
   as they only need the c register and they ignore a and b
*/

void read(uint32_t a, uint32_t b, uint32_t c, registers[]);

void write(uint32_t a, uint32_t b, uint32_t c, registers[]);

#endif
